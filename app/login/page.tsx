'use client';

export default function LoginPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-primary">
      <div className="w-full max-w-md text-center p-8">
        <h2 className="text-2xl font-semibold text-text-primary mb-6">Login removed</h2>
        <p className="text-muted">This project no longer uses authentication — all pages are public.</p>
      </div>
    </div>
  );
}
